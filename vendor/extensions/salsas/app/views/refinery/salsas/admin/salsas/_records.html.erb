<%= render 'refinery/admin/search_header', :url => refinery.salsas_admin_salsas_path %>
<div class='pagination_container'>
  <% if @salsas.any? %>
    <%= render 'salsas' %>
  <% else %>
    <p>
      <% unless searching? %>
        <strong>
          <%= t('.no_items_yet') %>
        </strong>
      <% else %>
        <%= t('no_results', :scope => 'refinery.admin.search') %>
      <% end %>
    </p>
  <% end %>
</div>
