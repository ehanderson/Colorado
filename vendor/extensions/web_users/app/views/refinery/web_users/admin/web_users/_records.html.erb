<%= render 'refinery/admin/search_header', :url => refinery.web_users_admin_web_users_path %>
<div class='pagination_container'>
  <% if @web_users.any? %>
    <%= render 'web_users' %>
  <% else %>
    <p>
      <% unless searching? %>
        <strong>
          <%= t('.no_items_yet') %>
        </strong>
      <% else %>
        <%= t('no_results', :scope => 'refinery.admin.search') %>
      <% end %>
    </p>
  <% end %>
</div>
