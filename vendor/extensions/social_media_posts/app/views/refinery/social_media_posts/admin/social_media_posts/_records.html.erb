<%= render 'refinery/admin/search_header', :url => refinery.social_media_posts_admin_social_media_posts_path %>
<div class='pagination_container'>
  <% if @social_media_posts.any? %>
    <%= render 'social_media_posts' %>
  <% else %>
    <p>
      <% unless searching? %>
        <strong>
          <%= t('.no_items_yet') %>
        </strong>
      <% else %>
        <%= t('no_results', :scope => 'refinery.admin.search') %>
      <% end %>
    </p>
  <% end %>
</div>
